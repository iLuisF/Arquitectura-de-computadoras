#include <stdlib.h>
#include "pila.c"

int main(){

  pila * p = new_pila(10);

  p->push(p, 11);
  p->push(p, 7);
  p->push(p, 35);

  printf("EL ultimo elemento removido: %d\n", p->pop(p));
  printf("EL ultimo elemento removido: %d\n", p->pop(p));
  printf("EL ultimo elemento removido: %d\n", p->pop(p));

  return 0;
}
